import ClassRoom from "./0-classroom.js";

// Create a new instance of ClassRoom with maxStudentsSize of 10
const room = new ClassRoom(10);

// Log the value of _maxStudentsSize to the console
console.log(room._maxStudentsSize); // Expected output: 10
